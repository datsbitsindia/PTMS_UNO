const bcrypt=require('bcrypt'); const {db}=require('../database/init'); const activity=require('../services/activityService');
exports.loginPage=async(req,res)=>{const demoAccounts=await db.prepare("SELECT name,email,role FROM users WHERE active=1 ORDER BY CASE role WHEN 'admin' THEN 1 WHEN 'employee' THEN 2 ELSE 3 END, name").all();res.render('login',{error:null,demoAccounts});};
exports.login=async(req,res,next)=>{try{const email=String(req.body.email||'').trim().toLowerCase();const user=await db.prepare('SELECT * FROM users WHERE email=? AND active=1').get(email);if(!user||!await bcrypt.compare(req.body.password||'',user.password)){const demoAccounts=await db.prepare("SELECT name,email,role FROM users WHERE active=1 ORDER BY CASE role WHEN 'admin' THEN 1 WHEN 'employee' THEN 2 ELSE 3 END, name").all();return res.status(401).render('login',{error:'Invalid email or password',demoAccounts});}req.session.regenerate(err=>{if(err)return next(err);req.session.user={id:user.id,name:user.name,email:user.email,role:user.role};req.session.save(async saveError=>{if(saveError)return next(saveError);try{await activity.log(user.id,'Login');}catch(logError){console.error('Login activity could not be recorded:',logError.message);}res.redirect(user.role === 'admin' ? '/projects' : user.role === 'employee' ? '/tasks' : '/dashboard');});});}catch(error){next(error);}};
exports.logout=(req,res)=>{const id=req.session.user?.id; activity.log(id,'Logout'); req.session.destroy(()=>res.redirect('/login'));};
